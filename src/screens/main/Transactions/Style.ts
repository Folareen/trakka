import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {},
    header: {
        flexDirection: 'row',
        padding: 15,
        alignItems: 'center'
    },
    backBtn: {

    },
    title: {
        flex: 1,
        textAlign: 'center',
        fontSize: 18,
        fontFamily: '600'
    }
})